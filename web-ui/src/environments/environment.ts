// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev mapStyleSettings which uses `mapStyleSettings.ts`, but if you do
// `ng build --env=prod` then `mapStyleSettings.prod.ts` will be used instead.
// The list of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
  production: false
};
